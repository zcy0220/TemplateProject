/**
 * 非运行的编辑模式下
 * 模拟运行状态
 * 主要是Update驱动和FixedUpdate驱动
 */

using UnityEngine;

namespace Editor.EditorModePlay
{
    public class EditorModePlayLauncher
    {
        public static void Start()
        {
            var fixedDeltaTime = (int)(Time.fixedDeltaTime * 1000);
            //while()
        }
    }
}
